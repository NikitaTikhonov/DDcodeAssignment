import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

interface Props {}

const VisualGuides = (props: Props): JSX.Element => {
  return <FlatList></FlatList>
}

export default VisualGuides

const styles = StyleSheet.create({})
